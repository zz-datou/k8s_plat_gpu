# 05_企业真实场景_实际怎么用

> 本文是场景总览。带证据链、只读命令、停止条件、回滚和脱敏真实案例的完整版本，见 [企业实战案例库](07_正式学习讲义/案例库/README.md)。
> 案例明确区分真实故障抽象、复合案例和 GPU 迁移演练，不把教学构造冒充事故复盘。

## 1. Java游戏业务SRE场景

### 场景一：游戏官网发布

企业里通常是：

```text
代码合并
  -> CI构建镜像
  -> 推送镜像仓库
  -> CD更新Deployment
  -> 滚动发布
  -> readiness通过
  -> Service/Ingress接入流量
  -> 监控确认
```

你要关注：

- 新版本Pod是否Ready。
- 老版本Pod是否过早退出。
- Ingress是否仍然指向正确Service。
- 静态资源、CDN、缓存是否同步。
- 错误率和延迟是否变化。

常见故障：

```text
ImagePullBackOff
Readiness失败
配置文件缺失
证书或域名错误
Service selector写错
Ingress规则冲突
```

### 场景二：游戏SDK服务异常

SDK服务往往更敏感，因为它可能影响登录、支付、埋点、认证、渠道接入。

你要关注：

- QPS。
- P95/P99延迟。
- 错误率。
- 依赖服务延迟。
- 连接池。
- JVM GC。
- HPA是否扩容。
- Pod是否频繁重启。

排查链路：

```text
业务告警
  -> Ingress/网关指标
  -> Service后端Endpoint
  -> Pod Ready和重启
  -> 应用日志
  -> JVM指标
  -> 数据库/Redis/第三方依赖
  -> 节点资源和网络
```

### 场景三：发布后接口变慢

不要只看Pod是否Running。

真实排查要看：

```text
新版本接口延迟
CPU throttling
GC时间
线程池队列
数据库连接池
下游依赖超时
Ingress 5xx
Pod跨可用区访问
```

K8s相关重点：

```text
request/limit是否变化
HPA target是否合理
readiness是否过早放流量
maxUnavailable是否导致容量下降
```

## 2. GPU运维场景

### 场景一：GPU Pod一直Pending

排查顺序：

```text
kubectl describe pod
  -> 看FailedScheduling event
  -> 看是否 Insufficient nvidia.com/gpu
  -> 看Pod是否有nodeSelector/affinity
  -> 看GPU节点taint和Pod toleration
  -> 看Node allocatable里是否有nvidia.com/gpu
  -> 看device plugin Pod是否正常
```

可能原因：

- 集群没有GPU节点。
- GPU节点没有上报 `nvidia.com/gpu`。
- GPU已经被其他Pod占满。
- 节点标签不匹配。
- taint未容忍。
- device plugin异常。
- GPU Operator组件异常。

### 场景二：Pod Running但容器内看不到GPU

排查顺序：

```text
Pod yaml是否声明 nvidia.com/gpu
  -> node是否有GPU
  -> device plugin是否Allocate成功
  -> containerd是否配置NVIDIA runtime
  -> 容器内nvidia-smi是否存在
  -> driver和CUDA兼容性
```

关键判断：

```text
调度成功只代表节点资源匹配。
容器内可见GPU还依赖kubelet device manager、device plugin Allocate和runtime注入。
```

### 场景三：GPU OOM

GPU OOM不是普通内存OOM。

排查要看：

- 模型大小。
- batch size。
- 上下文长度。
- 并发数。
- KV cache。
- 显存碎片。
- 是否多个进程共享同卡。
- MIG/time-slicing配置。

K8s层面要看：

```text
Pod是否重启
容器退出码
应用日志
DCGM显存指标
GPU进程残留
```

### 场景四：GPU利用率低

低利用率不一定等于故障，但企业会关心成本。

可能原因：

- 请求流量低。
- batch太小。
- CPU预处理瓶颈。
- 数据加载瓶颈。
- 模型服务并发配置低。
- 显存占满但计算利用率低。
- 调度粒度太粗，一张卡只跑一个低负载服务。

优化方向：

```text
提高batch或并发
拆分模型服务规格
使用MIG或time-slicing
把离线任务和在线推理分池
用队列系统提高批任务资源利用率
```

### 场景五：多团队共享GPU

企业真实问题：

```text
算法团队想训练。
业务团队想部署推理。
测试团队想临时调试。
老板关心GPU钱花得值不值。
```

平台要提供：

- 命名空间隔离。
- ResourceQuota。
- PriorityClass。
- 队列。
- GPU型号选择。
- 成本归属。
- 空闲回收。
- 审批和配额调整。

## 3. 两条路线的典型排障对照

| 问题 | Java业务SRE | GPU运维 |
| --- | --- | --- |
| Pod Pending | CPU/memory不足、亲和性、污点 | GPU不足、GPU节点标签、device plugin未上报 |
| Pod Running但服务不可用 | readiness、端口、Service、Ingress | 模型未加载、GPU不可见、显存不足 |
| OOM | JVM堆/非堆/容器内存 | GPU显存、batch、模型大小、KV cache |
| 扩容无效 | HPA指标、request、启动慢 | GPU卡数不足、模型冷启动、单卡并发瓶颈 |
| 成本高 | request虚高、节点空闲 | GPU空闲、低利用率、型号不匹配 |
| 发布失败 | Deployment/ReplicaSet/readiness | 镜像大、模型下载慢、GPU初始化失败 |

## 4. 企业更看重什么

### Java业务SRE方向

企业看重：

```text
稳定性
发布效率
故障恢复
监控告警
容量规划
安全合规
平台规范
```

能体现能力的产出：

- 标准Deployment模板。
- Java服务资源配置规范。
- 发布回滚SOP。
- Ingress/Service排障手册。
- HPA配置规范。
- JVM容器化排障手册。

### GPU运维方向

企业看重：

```text
GPU资源可用
GPU资源高效
模型服务稳定
多租户公平
成本可解释
故障能定位
```

能体现能力的产出：

- GPU节点池建设文档。
- GPU Pod调度排障手册。
- Device Plugin链路说明。
- DCGM监控看板。
- vLLM服务部署模板。
- GPU利用率和成本报表。
- 多租户队列和配额方案。

