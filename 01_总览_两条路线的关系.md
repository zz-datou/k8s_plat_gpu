# 01_总览_两条路线的关系

## 1. 你现在的位置

你现在的本职工作可以概括成：

```text
SRE平台 / Kubernetes运维工程师
  -> 运维公司 Java 容器应用
  -> 支撑游戏官网、主页、SDK、业务后端服务
  -> 保障发布、稳定性、容量、故障恢复、监控告警
```

你想逐步转向：

```text
GPU运维 / AI算力平台工程师
  -> 运维 GPU Kubernetes 集群
  -> 支撑推理、训练、模型服务、算力资源池
  -> 保障 GPU调度、设备健康、显存、利用率、成本
```

这两条线不是割裂的。它们的共同底座是：

```text
Kubernetes + Linux + 容器运行时 + 网络 + 存储 + 监控 + SRE故障处理
```

区别在于：

```text
Java业务SRE管理的是“在线应用生命周期”。
GPU运维管理的是“昂贵算力资源生命周期”。
```

## 2. 两条路线的共同点

### 共同点一：都绕不开 Pod 生命周期

Java业务场景：

```text
Deployment 创建 Pod
  -> scheduler 选节点
  -> kubelet 启动容器
  -> readiness 通过
  -> Service/Ingress 接流量
  -> HPA/发布/回滚/扩缩容
```

GPU场景：

```text
Job/Deployment 创建 GPU Pod
  -> scheduler 判断 nvidia.com/gpu 是否足够
  -> kubelet 调 device plugin 分配 GPU
  -> runtime 注入 GPU 设备
  -> 模型服务启动
  -> DCGM/Prometheus 监控 GPU 指标
```

所以 Pod 主线、scheduler、kubelet 是两条路线的共同核心。

### 共同点二：都要会排障

Java业务常见问题：

```text
ImagePullBackOff
CrashLoopBackOff
Readiness失败
Service无后端
Ingress 404/502/504
CPU throttling
OOMKilled
HPA不扩容
滚动发布卡住
```

GPU业务常见问题：

```text
GPU Pod Pending
nvidia.com/gpu 不存在
device plugin down
容器内 nvidia-smi 不可用
GPU OOM
driver crash
GPU利用率低
模型服务延迟抖动
多租户抢占资源
```

底层排查方法类似：

```text
kubectl describe
events
logs
node状态
runtime状态
资源请求
监控指标
组件日志
```

### 共同点三：都要做平台规范

Java业务平台规范：

```text
Deployment模板
Service/Ingress规范
probe规范
requests/limits规范
日志采集规范
监控告警规范
发布回滚规范
命名空间和权限规范
```

GPU平台规范：

```text
GPU节点池规范
GPU Pod模板
device plugin / GPU Operator规范
DCGM指标规范
GPU队列和配额规范
模型服务部署规范
显存和并发规范
成本与利用率规范
```

两者本质都是：

```text
把散乱的运维经验沉淀成平台规则。
```

## 3. 两条路线的不同点

### Java业务SRE看的是应用稳定性

主要问题是：

```text
服务能不能发布？
流量能不能进来？
实例是否Ready？
接口延迟是否正常？
扩缩容是否稳定？
故障能不能快速恢复？
```

核心资源是：

```text
CPU / Memory / 网络 / 连接数 / JVM / 数据库连接 / QPS / 延迟
```

### GPU运维看的是算力资源效率

主要问题是：

```text
GPU是否能被K8s识别？
GPU Pod为什么调度不上？
容器为什么看不到GPU？
显存为什么不够？
GPU利用率为什么低？
多个团队如何共享GPU？
GPU成本怎么控制？
```

核心资源是：

```text
GPU卡数 / GPU型号 / 显存 / SM利用率 / 显存利用率 / PCIe/NVLink / NCCL / 模型并发 / token吞吐
```

## 4. 学习策略

你的学习不能平均用力。建议用三层来看：

```text
第一层：两条路线都必须深入
第二层：当前Java业务SRE要深入，GPU只需迁移理解
第三层：GPU方向必须深入，Java业务只需知道边界
```

### 第一层：共同底座，必须深入

这里的“深入”优先指运维、机制和排障能力，不代表每一项都要源码深读。具体源码等级统一看 `04_K8s交叉能力矩阵_哪些深入哪些一笔带过.md`。

- Pod 生命周期
- Deployment / ReplicaSet / Job
- scheduler 调度主链路
- kubelet 执行主链路
- requests / limits / QoS
- Service / EndpointSlice / kube-proxy / Ingress
- ConfigMap / Secret
- probe / status / event
- HPA / metrics
- Linux / containerd / image / cgroup
- Prometheus / Grafana / logs / alerts
- RBAC / namespace / quota

### 第二层：Java业务SRE深入

- 滚动发布、回滚、灰度、蓝绿。
- JVM容器化参数。
- Spring Boot健康检查。
- Ingress网关、TLS、域名、WAF/CDN边界。
- 业务监控：QPS、延迟、错误率、可用性。
- SDK类服务的兼容性、版本、限流、熔断。

### 第三层：GPU方向深入

- NVIDIA Driver / CUDA / Container Toolkit。
- Kubernetes Device Plugin。
- NVIDIA GPU Operator。
- kubelet device manager。
- `nvidia.com/gpu` extended resource。
- DCGM Exporter。
- vLLM / Triton / KServe。
- MIG / time-slicing。
- Kueue / Volcano / 多租户队列。
- GPU成本治理。

## 5. 最重要的转型判断

不要把 GPU 运维理解成“我要去训练模型”。

更准确的定位是：

```text
你要从普通业务平台SRE，升级成能管理昂贵异构资源的Kubernetes平台工程师。
```

你最值钱的能力组合会是：

```text
Kubernetes源码理解
+ 生产SRE经验
+ GPU资源调度与排障
+ AI模型服务平台运维
+ 成本治理
```
