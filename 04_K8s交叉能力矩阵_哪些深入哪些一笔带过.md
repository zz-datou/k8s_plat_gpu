# 04_K8s交叉能力矩阵_哪些深入哪些一笔带过

> 2026-07-12 重构说明：这份文档按“你本人真正要掌握什么”重新编排，不再按讲义数量衡量进度。`study/90_主线复盘与进阶/01~1267` 是资料库，不是必读课表。

## 1. 先给结论

你的目标岗位第一阶段是：

```text
平台 Kubernetes 运维
  -> GPU Kubernetes 基础设施
  -> 在线推理平台 SRE
```

你不需要通读 Kubernetes 源码。真正值得源码深读的只有三条主线：

```text
1. scheduler 资源调度
2. kubelet 节点执行与 CRI 边界
3. Device Plugin / DeviceManager / GPU 注入
```

Controller / Operator 要理解控制回路和关键入口；只有以后自己开发 GPU 平台控制器时，才升级为源码深读。

其余大部分知识都应该遵循：

```text
运维能力学深
机制理解学透
Kubernetes 源码学到够定位即可
```

源码学习的验收标准也不是“看过多少文件”，而是发生故障时能判断问题属于：

```text
apiserver
scheduler
kubelet
containerd / CRI
Device Plugin
Driver / Toolkit
模型服务
```

## 2. 当前路线边界

### 第一阶段主线

- 平台 Kubernetes 生产运维。
- GPU 节点池和 NVIDIA 容器栈。
- GPU 在 Kubernetes 中的声明、调度、分配、注入和观测。
- GPU Operator 与 DCGM。
- vLLM 在线推理服务。
- 基础多租户、配额、队列和成本治理。

### 第二阶段再学

- 分布式训练。
- NCCL、RDMA、InfiniBand/RoCE。
- 多机多卡拓扑。
- GPUDirect、并行存储、训练数据管道。
- 深入 Kueue/Volcano、gang scheduling 和训练平台。

### 当前明确不是主线

- CUDA kernel 开发。
- PyTorch 算子或训练框架源码。
- NVIDIA Driver 源码。
- 把所有 Kubernetes 内部实现逐文件读完。

## 3. 用两个维度判断学习深度

“运维要深入”和“源码要深入”不是一回事。

### 3.1 运维深度

| 级别 | 含义 | 完成标准 |
| --- | --- | --- |
| O3 独立负责 | 必须深入 | 能设计、上线、监控、变更、回滚、排障和复盘 |
| O2 熟练使用 | 要熟练 | 能正确配置并处理常见故障，复杂问题会继续收集证据 |
| O1 理解边界 | 先了解 | 知道用途、依赖、风险和什么时候需要深入 |

### 3.2 源码深度

| 级别 | 含义 | 完成标准 |
| --- | --- | --- |
| S3 源码深读 | 少数核心闭环 | 能沿真实调用链找到入口、核心状态、失败点、重试和恢复点，并结合 Event/日志/指标解释生产现象 |
| S2 机制 + 关键源码 | 重要机制 | 能画状态机或控制回路，知道关键入口函数，必要时能再追一到两层 |
| S1 源码浅读 | 运维优先 | 知道组件边界和少量入口；日常主要靠 API、配置和运行证据排障 |
| S0 不读源码 | 只学使用或概念 | 会部署、配置或知道边界；只有岗位变化时再补源码 |

以后不能只说“这个要深入”，而要明确是 `O3/S1` 还是 `O3/S3`。

## 4. 平台 K8s 能力如何迁移到 GPU

| 当前平台 K8s 能力 | 当前工作怎么用 | GPU 方向怎么迁移 | 运维/源码深度 |
| --- | --- | --- | --- |
| Pod 生命周期 | 定位 Pending、Creating、CrashLoop、NotReady | 定位 GPU Pod 从声明到容器可见 GPU | O3 / S2，设备段升到 S3 |
| Deployment / ReplicaSet | Java 服务发布、回滚、灰度 | vLLM 在线推理服务发布和回滚 | O3 / S1~S2 |
| Job / CronJob | 批处理、定时任务 | 离线推理和训练任务 | O2 / S1；训练阶段再升深 |
| probe / status / event | 判断 Running 是否真正可用 | 判断模型是否加载完成、是否能接流量 | O3 / S2 |
| requests / limits / QoS | CPU、内存容量和驱逐治理 | `nvidia.com/gpu`、CPU/内存/GPU 联合资源设计 | O3 / S3（调度段） |
| Service / EndpointSlice / Ingress | 官网、SDK 和 Java 服务流量 | 推理 API、网关、超时和长连接 | O3 / S1~S2 |
| HPA / metrics | 按 CPU、内存、QPS 扩容 | 按队列、并发、延迟、tokens/s 扩容 | O3 / S1~S2 |
| Node Pool | 普通业务节点分池 | GPU 型号、Spot/按需、在线/离线分池 | O3 / S2 |
| taint / affinity | 工作负载隔离与定向调度 | GPU 专用节点、型号和业务类型约束 | O3 / S2 |
| containerd / CRI | 镜像、Sandbox、runtime 排障 | NVIDIA Container Toolkit、CDI 和 GPU 注入 | O3 / S2~S3 |
| Prometheus / Grafana | 主机、容器和业务监控 | DCGM、GPU health、显存、利用率和成本 | O3 / S0（K8s 源码） |
| Namespace / RBAC / Quota | 团队隔离和权限 | GPU 租户、配额、优先级和成本归属 | O3 / S1 |
| Controller / Operator | 理解平台自动化 | 运维 GPU Operator，后续开发 GPU 治理控制器 | O2 / S2；二开时升 S3 |

## 5. 三条必须源码深读的主线

### 5.1 scheduler 资源调度主线：O3 / S3

#### 必须掌握的链路

```text
未绑定 Pod
  -> 调度队列
  -> ScheduleOne / schedulingCycle
  -> PreFilter / Filter / Score
  -> NodeResourcesFit
  -> assume
  -> bind
  -> 成功，或 FailedScheduling 后重新入队
```

#### 必须讲清的概念

- scheduler 看的是 request、Node allocatable 和已经承诺的 request，不是实时 CPU/GPU 利用率。
- `nvidia.com/gpu` 是 extended resource，通常按整数申请，不能像 CPU 那样超卖。
- namespace 配额通常按 `requests.nvidia.com/gpu` 统计 extended resource 预算，不把裸资源名或 `limits.nvidia.com/gpu` 当配额键。
- scheduler 只决定 Pod 去哪台 Node，不选择具体 GPU UUID。
- taint/toleration、node affinity、nodeSelector、topology spread 和资源数量是不同过滤条件。
- Unschedulable、backoff、事件唤醒和抢占分别解决什么问题。

#### 关键源码入口

```text
kubernetes/pkg/scheduler/schedule_one.go
kubernetes/pkg/scheduler/framework/plugins/noderesources/fit.go
kubernetes/pkg/scheduler/backend/queue/
kubernetes/pkg/scheduler/framework/plugins/defaultbinder/
```

#### 停止边界

第一阶段不需要读完：

- 所有 scheduler 插件。
- Extender 全实现。
- cache/snapshot 每个一致性细节。
- 异步调度 API 和全部实验性特性。
- 每个打分公式的所有分支。

只有以后维护自定义 scheduler、调度插件或训练队列时才继续深挖。

#### 验收标准

给你一条 `FailedScheduling` Event，你能判断是：

```text
资源数量不足
request 计算问题
taint 没有 toleration
affinity / selector 不匹配
拓扑约束
PVC / VolumeBinding
优先级和抢占
调度队列等待或退避
```

并能解释“GPU 利用率看起来很低，为什么新 Pod 仍然调度不上”。

### 5.2 kubelet 节点执行与 CRI 边界：O3 / S3

#### 必须掌握的链路

```text
scheduler 写入 spec.nodeName
  -> kubelet watch 到本节点 Pod
  -> syncLoop
  -> podWorkers
  -> Kubelet.SyncPod
  -> volume / cgroup / probe 准备
  -> kubeGenericRuntimeManager.SyncPod
  -> createPodSandbox / startContainer
  -> CRI 调用 containerd
  -> probe / statusManager 回写状态
```

#### 必须讲清的概念

- `nodeName` 有值只代表调度完成，不代表容器已经启动。
- kubelet、containerd、runc 和 CNI 分别负责哪一层。
- Sandbox、业务容器、镜像、卷、网络、探针和状态回写的先后关系。
- GPU 注入发生在调度之后、容器创建之前。
- 哪些失败会表现为 ContainerCreating、CreateContainerError、CrashLoopBackOff 或 Running but NotReady。

#### 关键源码入口

```text
kubernetes/pkg/kubelet/kubelet.go
kubernetes/pkg/kubelet/pod_workers.go
kubernetes/pkg/kubelet/kuberuntime/
kubernetes/staging/src/k8s.io/cri-api/
```

#### 停止边界

第一阶段不需要逐行读：

- PLEG 所有实现细节。
- ImageGC、ContainerGC 的每个策略分支。
- 原地扩缩容全部状态机。
- checkpoint container、Pods API 等低频接口。
- kubelet stats 的每个边角字段。

这些只在对应故障出现时回查。

#### 验收标准

看到以下状态时，能决定先查哪个组件和哪类证据：

```text
Pending
ContainerCreating
ImagePullBackOff
CreateContainerError
CrashLoopBackOff
Running but NotReady
NodeNotReady
```

### 5.3 Device Plugin / DeviceManager / GPU 注入：O3 / S3

#### 必须掌握的链路

```text
NVIDIA Device Plugin 启动 gRPC 服务
  -> 向 kubelet 注册 resourceName 和 socket
  -> ListAndWatch 上报设备与健康状态
  -> kubelet DeviceManager 更新 capacity / allocatable
  -> scheduler 按 nvidia.com/gpu 数量选 Node
  -> kubelet 为容器选择具体 device ID
  -> 调用 Allocate
  -> 插件返回 env / device / mount / annotation / CDI
  -> DeviceManager 合并 RunContainerOptions
  -> CRI/runtime 创建容器
  -> PodResources 暴露 Pod/container 到 device ID 的归属
```

#### 必须讲清的概念

- Device Plugin 不是 scheduler 插件。
- `nvidia.com/gpu` 是资源名，GPU UUID/device ID 是节点侧具体设备，两者不是一回事。
- capacity、allocatable、healthy/unhealthy devices 的关系。
- 插件断连、kubelet 重启和 checkpoint 恢复会发生什么。
- `Allocate` 为什么会影响容器内是否可见 GPU。
- PodResources API 为什么对按 Pod 归属 GPU 指标有用。

#### 关键源码入口

```text
kubernetes/pkg/kubelet/cm/devicemanager/manager.go
kubernetes/pkg/kubelet/cm/devicemanager/pod_devices.go
kubernetes/pkg/kubelet/cm/devicemanager/endpoint.go
kubernetes/pkg/kubelet/cm/container_manager_linux.go
kubernetes/pkg/kubelet/apis/podresources/server_v1.go
kubernetes/staging/src/k8s.io/kubelet/pkg/apis/deviceplugin/v1beta1/api.proto
```

#### 停止边界

第一阶段不需要：

- 逐行读完整 NVIDIA Device Plugin 仓库。
- 逐行读 GPU Operator 所有 controller 和 CRD。
- 深读 DRA Manager、ResourceClaim 和全部驱动实现。
- 立即研究所有 CDI、NRI 或新特性分支。

先把传统 Device Plugin + extended resource 闭环跑通；目标集群真正采用 DRA 时再升级。

#### 验收标准

能把这些故障分层定位：

| 现象 | 首先怀疑的层 |
| --- | --- |
| Node 没有 `nvidia.com/gpu` | Driver、Toolkit、Device Plugin、ListAndWatch、Node status |
| GPU Pod 一直 Pending | request、allocatable、taint、affinity、节点池、队列 |
| Pod 已调度但 Allocate 失败 | kubelet DeviceManager、插件 endpoint、设备健康、checkpoint |
| 容器内没有 GPU | Allocate 返回、CDI/env/device/mount、containerd runtime 配置 |
| GPU 指标无法归属到 Pod | PodResources、GPU UUID 映射、DCGM 指标标签 |
| 插件重启后资源异常 | socket 重注册、ListAndWatch、checkpoint 和 allocatable 收敛 |

## 6. 机制要学透，但不必源码深读

| 知识点 | 建议深度 | 需要掌握 | 不必深入 |
| --- | --- | --- | --- |
| Pod 从 API 到 Running | O3 / S2 | 组件边界、状态时间线、Event/日志定位 | apiserver 每个 RESTStorage |
| informer/workqueue/controller | O2 / S2 | watch、cache、queue、幂等 reconcile、重试 | 每个内置 controller |
| Deployment/ReplicaSet | O3 / S2 | rollout、available/progress、ownerReference、回滚 | 所有边界分支 |
| Job/CronJob | O2 / S1~S2 | completion、parallelism、失败重试、清理 | 训练阶段前不深挖 controller |
| authn/authz/admission | O3 / S1 | 401、403、准入拒绝、webhook timeout | generic apiserver 装配 |
| Service/EndpointSlice/readiness | O3 / S2 | Ready Pod 如何进入后端、入口故障分层 | kube-proxy 所有规则生成代码 |
| NodeStatus/Lease/eviction | O3 / S2 | Ready、Pressure、失联、驱逐、节点隔离 | 每个 signal 计算分支 |
| HPA/metrics/CA | O3 / S1~S2 | 指标来源、副本计算、节点扩容边界 | 所有 metrics client 实现 |
| Quota/PDB/Priority | O3 / S1~S2 | 创建拒绝、drain、抢占、租户治理 | 所有 admission evaluator |
| CRD/Operator | O2 / S2 | 控制回路、status、finalizer、RBAC | 不做二开时不深读 API Machinery |
| TopologyManager | O2 / S2 | NUMA hint、policy、scope、准入结果 | 没有低延迟/多卡需求时不读所有策略 |
| CPUManager/MemoryManager | O1~O2 / S1~S2 | Guaranteed、static policy、cpuset、NUMA | 需要绑核/绑内存时再深入 |
| DRA/ResourceClaim | O1 / S1 | 与 Device Plugin 的差异和使用边界 | 集群未采用时不读实现 |

## 7. 运维必须熟练，但 K8s 源码只需浅看

### 7.1 平台 Kubernetes

| 领域 | 必须达到的运维能力 | 源码要求 |
| --- | --- | --- |
| Deployment、DaemonSet、StatefulSet、Job | 会选型、发布、回滚、失败恢复；知道 GPU 插件为何常用 DaemonSet | S1 |
| probe、优雅终止、PDB、drain | 能做无损发布和节点维护演练 | S1~S2 |
| CNI、Service、Ingress/Gateway、DNS | 能按入口、Service、Endpoint、Pod、网络分层收集证据 | S0~S1 |
| PV/PVC/StorageClass/CSI | 能排 PVC Pending、Attach/Mount、拓扑和模型加载慢 | S0~S1 |
| RBAC、ServiceAccount、Pod Security、Secret | 能设计最小权限、租户隔离和凭据轮换 | S0~S1 |
| control plane HA、etcd、证书、升级 | 能备份恢复、兼容性检查、证书轮换和滚动升级 | S0 |
| Prometheus、Grafana、Alertmanager、日志、审计 | 能建设节点、工作负载、控制面和 SLO 看板 | S0 |
| Helm、GitOps、CI/CD | 能标准化安装、变更、回滚和审计 | S0 |

### 7.2 GPU 专项

| 领域 | 必须达到的运维能力 | K8s 源码要求 |
| --- | --- | --- |
| Driver / CUDA / Container Toolkit / containerd/CDI | 能在宿主机和容器分别验证 GPU，区分驱动、用户态库和 runtime 注入问题 | S0；配置和日志要学深 |
| GPU Operator | 能说明 Driver、Toolkit、Device Plugin、DCGM、MIG Manager、NFD/GFD/Validator 的职责，能按 Pod/DaemonSet 定位故障 | S1 |
| GPU 节点池 | 能做型号标签、taint/toleration、affinity、RuntimeClass、升级、隔离和自动扩缩容 | S1~S2 |
| DCGM 与 GPU 健康 | 能建设 GPU/节点/Pod/业务四层指标和告警，能处理 Xid、ECC、温度、功耗异常 | S0 |
| MIG / time-slicing / MPS | 先选择一种实验，能解释隔离、故障域、监控和计费风险 | S0~S1 |
| vLLM | 能部署、探针、压测和回滚，解释显存、KV cache、tokens/s、并发和延迟 | S0 |
| 推理扩缩容 | 优先使用队列、并发、延迟、tokens/s 等业务信号，不机械照搬 GPU util | S0~S1 |
| 多租户与成本 | 能按团队统计分配时长、有效利用率、空闲率和吞吐，设计 quota/priority/节点池/队列 | S0~S1 |
| 模型镜像与存储 | 能治理大镜像、模型下载、缓存、启动风暴、PVC/对象存储和本地盘 | S0 |

## 8. 当前可以一笔带过或跳过的源码

- kube-apiserver 启动参数和 generic server 委托链逐文件细读。
- 每种资源的 RESTStorage、Strategy、conversion、Scheme、Codec 实现。
- etcd Raft、MVCC、WAL 源码；但运维上仍要会备份、恢复、压缩、碎片整理和监控延迟。
- 所有内置 controller 源码。
- scheduler 所有插件、Extender、异步 API、cache/snapshot 的全部细节。
- kube-proxy iptables/IPVS/nftables 的全部规则生成源码。
- CNI、CSI、Ingress Controller 的完整源码。
- kubelet checkpoint container、Pods API、原地扩缩容、全部 GC 和 stats 分支。
- Windows、测试夹具、生成代码和 `vendor/`。
- NVIDIA Driver 源码、CUDA kernel、PyTorch 算子和训练框架源码。
- GPU Operator 每个 CRD/controller 的完整实现。
- 同时学习 vLLM、Triton、KServe、Ray Serve、Seldon；第一阶段只选 vLLM。
- 在线推理第一阶段就深挖 NCCL、RDMA、GPUDirect 和 gang scheduling。

## 9. 现有讲义的最小阅读路径

### 9.1 阅读原则

```text
先读总复盘建立地图
再做实验和故障
只有解释不了时进入专题源码
不按编号顺序通读
```

Go 也不需要先学成开发专家。先补足这些阅读能力：

```text
struct / method / interface / pointer
if err / defer / context
goroutine / channel / mutex
slice / map
```

对应讲义优先看：

- `study/01_Go必备补课/01_Go源码阅读必备语法与K8s惯用法.md`
- `study/01_Go必备补课/02_Go第一课_指针方法接口与嵌入.md`
- `study/01_Go必备补课/03_Go第二课_context_defer_goroutine.md`

`00_Go零基础入门_语法功能场景练习.md` 很长，只作为不懂语法时的字典，不要求从头背完。

### 9.2 第一圈：进入 GPU 前的 17 篇核心材料

#### 平台共同底座

1. `90_主线复盘与进阶/17_Pod主线全链路复盘_从kubectl到Service转发.md`
2. `90_主线复盘与进阶/24_Deployment主线全链路复盘_从Deployment到PodReady.md`
3. `90_主线复盘与进阶/29_Service主线全链路复盘_从Service到Pod转发.md`
4. `90_主线复盘与进阶/744_进阶专题_Batch主线总复盘_从kubectl到CronJob_Job_Controller.md`
5. `90_主线复盘与进阶/66_进阶专题_自动伸缩生态边界_HPA_VPA_CA.md`
6. `90_主线复盘与进阶/1124_进阶专题_节点维护闭环总复盘_cordon_drain_scheduler_PDB_ready.md`

#### scheduler 核心

7. `90_主线复盘与进阶/67_进阶专题_scheduler_Pending到Unschedulable排障链路.md`
8. `90_主线复盘与进阶/68_进阶专题_scheduler_NodeResourcesFit与资源请求计算.md`
9. `90_主线复盘与进阶/1164_进阶专题_scheduler资源调度总复盘_Profile_Plugin_Filter_Score_Event闭环.md`

#### kubelet 核心

10. `90_主线复盘与进阶/1245_进阶专题_kubelet必学边界_从NodeName到容器运行.md`
11. `90_主线复盘与进阶/1260_进阶专题_kubelet节点执行主线总复盘_从NodeName到PodRunning.md`
12. `90_主线复盘与进阶/1263_进阶专题_CRI远程服务边界_从kubeGenericRuntimeManager到containerd.md`
13. `90_主线复盘与进阶/1267_进阶专题_kubelet必学收口地图_哪些必须回看哪些可以暂缓.md`

#### GPU 设备核心

14. `90_主线复盘与进阶/999_进阶专题_DevicePlugin注册_registerEndpoint与ListAndWatch链路.md`
15. `90_主线复盘与进阶/1000_进阶专题_DeviceManager_allocate设备分配与podDevices状态链路.md`
16. `90_主线复盘与进阶/1001_进阶专题_DeviceManager_checkpoint恢复与健康状态更新链路.md`
17. `90_主线复盘与进阶/1004_进阶专题_PodResources_DevicePlugin_DRA总复盘_节点设备资源闭环.md`

这 17 篇也不是先全部读完才允许做 GPU；应与 `06` 的实验阶段交叉进行。

### 9.3 第二圈：出现对应需求再读

#### 调度资源计算细节

- `1135_进阶专题_NodeResourcesFit再深读_PreFilter如何把Pod转换成资源向量.md`
- `1139_进阶专题_NodeResourcesFit_Filter_fitsRequest不足资源判断链路.md`
- `1140_进阶专题_NodeResourcesFit扩展资源_ignoredResources_ignoredResourceGroups与DRA委托.md`
- `1141_进阶专题_NodeResourcesFit_Unschedulable与Unresolvable状态码含义.md`
- `1184_进阶专题_scheduler队列总复盘_activeQ_backoffQ_unschedulablePods_Event闭环.md`
- `1204_进阶专题_scheduler抢占总复盘_PostFilter_Candidate_Victim_Nominated闭环.md`

#### kubelet 断点细读

- `1246_进阶专题_kubelet_Run与syncLoop_节点侧主循环怎么启动.md`
- `1249_进阶专题_podWorkers_UpdatePod_每个Pod一个串行状态机.md`
- `1251_进阶专题_Kubelet_SyncPod总入口_准入_cgroup_目录_卷_探针_runtime.md`
- `1253_进阶专题_kubeGenericRuntimeManager_SyncPod_九步把Pod变成Sandbox和容器.md`
- `1254_进阶专题_createPodSandbox与startContainer_为什么先Pause再业务容器.md`
- `1258_进阶专题_probeManager三类探针_liveness_readiness_startup如何影响Pod.md`
- `1259_进阶专题_statusManager_patch_status_为什么Pod状态是最终一致.md`
- `1265_进阶专题_NodeStatus_节点Ready与Pressure状态如何上报.md`

#### 设备、监控与拓扑

- `995_进阶专题_KubeletPodResources_gRPC服务与设备资源上报链路.md`
- `996_进阶专题_PodResources_List_GetAllocatableResources与Get接口链路.md`
- `155_进阶专题_kubelet_TopologyManager与NUMA拓扑准入链路.md`
- `1014_进阶专题_Kubelet资源准入总复盘_allocation_topology_cpu_memory闭环.md`
- `60_进阶专题_kubelet_stats_summary与cAdvisor指标链路.md`
- `1094_进阶专题_HPA主线总复盘_从kubelet指标到scale子资源闭环.md`

#### Controller / Operator 基础

- `study/06_Informer与Reflector/01_ReflectorDeltaFIFO和Informer.md`
- `study/07_WorkQueue与控制器模板/01_WorkQueue与控制器模板.md`
- `study/08_sample-controller/01_sample-controller完整回路.md`
- `study/14_CRD/01_CRD动态路由处理器.md`
- `study/12_Admission/03_Mutating和ValidatingWebhook.md`

### 9.4 第三圈：明确采用后再学

- DRA：`1002~1003` 以及 ResourceClaim 相关专题。
- CPU/内存/NUMA 精细化：`1011~1014`。
- 训练队列：Kueue 或 Volcano 先选一个。
- GPU 共享：MIG、time-slicing、MPS 先按硬件和租户需求选一个。
- 分布式训练：只有岗位或项目明确需要时再进入 NCCL/RDMA/多机多卡。

## 10. 从平台 K8s 进入 GPU 的验收门槛

不要求先读完源码，但至少要能独立处理：

1. Deployment 发布卡住、回滚和新旧 Pod 比例异常。
2. Pod 因资源、taint、affinity 或 selector 而 Pending。
3. ContainerCreating、ImagePullBackOff、CrashLoopBackOff、NotReady。
4. Service 无 Endpoint、Ingress/Gateway 502/504。
5. Node NotReady、Memory/DiskPressure、cordon/drain 被 PDB 卡住。
6. PVC Pending、Attach/Mount 和存储拓扑问题。
7. RBAC、ResourceQuota 或 admission webhook 拒绝。
8. HPA 已扩 Pod 但节点资源不足，或 CA/HPA 没按预期工作。

这里的“能处理”至少包含：

```text
复现或模拟一次
收集 kubectl / Event / 日志 / 指标证据
画出责任组件链路
给出处理和回滚动作
写一页简短复盘
```

## 11. 学习和版本规则

- 当前本地源码是上游 `master` 快照，不等于你的生产版本。
- 每次源码专项先确认目标集群 Kubernetes 版本，再切到对应 tag/branch 校准文件和行为。
- 新功能是否必学以目标集群是否启用、岗位是否使用为准，不以 `master` 中是否存在为准。
- 优先看对象状态、Event、组件日志和指标；只有这些证据解释不了时再进源码。
- 每读一段源码必须绑定一个真实故障或实验，否则只算资料浏览。
- 每个阶段至少留下配置、验证结果、故障记录和复盘，不再只留下概念讲义。

## 12. 官方资料入口

- Kubernetes Device Plugins: <https://kubernetes.io/docs/concepts/extend-kubernetes/compute-storage-net/device-plugins/>
- Kubernetes Extended Resources: <https://kubernetes.io/docs/tasks/configure-pod-container/extended-resource/>
- Kubernetes Taints and Tolerations: <https://kubernetes.io/docs/concepts/scheduling-eviction/taint-and-toleration/>
- NVIDIA GPU Operator: <https://docs.nvidia.com/datacenter/cloud-native/gpu-operator/latest/>
- Kueue Concepts: <https://kueue.sigs.k8s.io/docs/concepts/>

最后只记一句：

```text
平台阶段把 scheduler、kubelet 学到能沿链路定位；
进入 GPU 后，把 NodeResourcesFit 和 Device Plugin/DeviceManager 提升到真正源码深读；
其余坚持运维熟练优先。
```
