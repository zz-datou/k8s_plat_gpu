# 06_接下来12周学习计划

> 2026-07-12 重构说明：这份计划以“个人真实掌握和可验证产出”为进度，不再以讲义是否生成或章节数量为进度。
>
> 阅读规则：每周真正要读的正文只从 `07_正式学习讲义/README.md` 进入。下面列出的旧源码文章只是我编写正式课时的范围索引，不是让你再跳回 `90_主线复盘与进阶` 自己拼课程。

## 0. 与冻结大纲的关系

`07_正式学习讲义/README.md` 是唯一课程顺序来源。本计划只安排时间和实验，不允许单独调整章节先后。

| 周次 | 对应冻结课程 |
|---:|---|
| 第 1 周 | 07 Deployment 源码热身 |
| 第 2 周 | 生产能力并行自测，不新增必修课，可 0 天通过 |
| 第 3 周 | 08～10 scheduler、资源模型、Pending/重入队/抢占 |
| 第 4 周 | 11～13 kubelet、CRI、probe/status |
| 第 5 周 | 14 NVIDIA Driver/CUDA 用户态/Container Toolkit/containerd/CDI |
| 第 6 周 | 15～17 Device Plugin、DeviceManager、PodResources/健康恢复 |
| 第 7 周 | 18 GPU Operator |
| 第 8 周 | 19 DCGM 与 GPU 健康 |
| 第 9～10 周 | 20 vLLM、压测、SLO 与扩缩容 |
| 第 11～12 周 | 21 MIG/time-slicing/队列/多租户/成本，以及综合演练 |

若以后确需改序，必须先改正式讲义 README 的版本和变更记录，再同步本表；不能反向用周计划改变课程顺序。

## 1. 12 周结束时要得到什么

目标不是再读几百篇 Kubernetes 源码，而是完成四个可以复现和展示的能力闭环：

```text
项目一：平台 K8s 发布、资源、调度和节点排障
项目二：GPU 节点池、Device Plugin、GPU Operator 和监控
项目三：vLLM 在线推理服务、压测、SLO 和故障处理
项目四：GPU 多租户、队列、共享和成本治理初版
```

第一阶段岗位定位：

```text
GPU Kubernetes 基础设施
+ 在线推理平台 SRE
```

分布式训练、NCCL/RDMA、多机多卡和 HPC 放到第二阶段。

## 2. 时间怎么分配

每周学习时间统一按下面比例，而不是原来含糊的“70% Java / 30% GPU”：

```text
50%：真实或模拟实验、故障注入、配置和验证
30%：平台 K8s / GPU 机制、官方文档和复盘
20%：与当前实验直接相关的 Kubernetes 源码
```

规则：

- 上述比例是整周“动手、阅读、复盘”的时间分配，不代表正式讲义只讲 20% 源码；08～13 的讲义仍以 Kubernetes 底层调用链、关键判断和失败流为主。
- 源码不是前置考试；实验做到哪，源码跟到哪。
- 同一周没有真实配置、命令输出、Event、日志或指标证据，就不能算完成。
- `study/90_主线复盘与进阶/01~1267` 由我按 `04` 的深度矩阵筛选并写入正式课；你不自行顺序查阅。
- 遇到 Go 语法卡点时再回查 Go 补课文档，不先花几个月把 Go 学成开发专家。

## 3. Week 0：开始 12 周前先确定环境

Week 0 不占 12 周，但必须先完成。没有环境基线，后面的 GPU Operator、DCGM 和 vLLM 都不可执行。

### 3.1 选择 GPU 来源

三选一：

| 方案 | 适合场景 | 取舍 |
| --- | --- | --- |
| 托管 Kubernetes GPU 节点池，例如 ACK | 最贴近云上平台运维和后续求职作品 | 成本较高，但节点池、扩缩容、云盘和网络更真实 |
| 一台 GPU 云主机 + 单节点/小型 Kubernetes | 先吃透 Driver、Toolkit、containerd、Device Plugin | 便宜、底层清楚，但平台能力较弱 |
| 本地/公司可用 GPU 节点 | 能长期做故障和升级实验 | 需要明确权限，不能影响生产 |

如果暂时没有 GPU：

- 第 1~4 周先在 kind、测试集群或现有平台完成普通 K8s 实验。
- 第 5 周前再准备按小时计费的 GPU 环境。
- 不要为了等 GPU 而停止平台 K8s 学习。

### 3.2 固定版本矩阵

建立 `环境与版本基线.md`，至少记录：

```text
Kubernetes 版本
Linux 发行版与 kernel
GPU 型号
NVIDIA Driver
CUDA 用户态版本 / 镜像
NVIDIA Container Toolkit
containerd
GPU Operator
NVIDIA Device Plugin
DCGM Exporter
vLLM
```

原则：

- 以目标平台和 GPU Operator 官方支持矩阵为准。
- 本地 Kubernetes 源码是上游 `master` 快照，只用于查机制；深读时切到目标集群对应 tag/branch。
- 不混用网上不同年份的安装命令和版本组合。

### 3.3 定义成本和销毁规则

- 记录每小时 GPU 费用和本周预算上限。
- 所有云资源必须有统一标签和销毁清单。
- 大模型、镜像、云盘和公网流量也要计入成本。
- 每次实验结束验证节点、云盘、负载均衡和公网 IP 是否已清理。

### Week 0 产出

```text
环境与版本基线.md
GPU实验预算与销毁清单.md
集群和GPU smoke test 结果.md
```

## 4. 第 1 周：用熟悉的 rollout 做源码反查热身

### 目标

不是重学 Deployment，而是把熟悉的发布现场第一次完整映射到 controller 源码。已有能力通过就快速前进，不必占满一周。

### 必须掌握

- event → queue → worker → `syncDeployment` 的控制器骨架。
- `syncDeployment -> rolloutRolling -> reconcileNew/OldReplicaSets`。
- maxSurge 扩新上限和 maxUnavailable 缩旧安全预算。
- Pod Ready 怎样经 ReplicaSet status 重新唤醒 Deployment。
- Available、Complete、Progressing、ProgressDeadlineExceeded 的源码边界。
- receiver、指针、多返回值和 early return 等本篇 Go 语法。

### 实验

1. 用现有 Java 服务案例或测试 Deployment 制造 readiness 失败。
2. 保存 Deployment/RS/Pod status、Condition、Event 时间线。
3. 手算为什么新 RS 只扩 1、旧 RS 一个都不能缩。
4. 在本地源码中找到关键 if，并把每个变量对应回 kubectl 字段。

### 正式课与源码范围

- `07_正式学习讲义/07_源码热身_Deployment_rollout卡住如何沿控制器找原因.md`
- `pkg/controller/deployment/deployment_controller.go`
- `pkg/controller/deployment/rolling.go`
- `pkg/controller/deployment/progress.go`
- `pkg/controller/replicaset/replica_set_utils.go`

### 验收产出

```text
Deployment rollout源码调用链图.md
Running_NotReady导致旧RS不缩故障复盘.md
生产字段到Go字段对照表.md
```

## 5. 第 2 周（并行自测，可 0 天通过）：生产能力校准

> 这是并行挑战闸门，不是固定学习周。下面五项都能准确解释时，按 0 天通过：不做实验、不生成缺口文档，完成 07 后立即开始正式课 08 scheduler。只有不确定的项目才选择性补。

### 必须掌握

- 能沿 Service selector → EndpointSlice → Pod IP → 节点数据面定位流量故障。
- 能精确解释 scheduler 为什么看 request 而不是实时 usage。
- 能解释 HPA CPU 利用率分母以及 HPA 与 Node Pool 扩容的边界。
- 能安全完成 cordon/drain，并说明 PDB 为什么会阻塞。
- 能从 Node Condition、Event、kubelet/runtime 日志判断节点责任层。

### 实验

五项全通过时不做实验。只有某一项解释不清，才针对该项选择一个场景补证据，不重复完成已熟练的基础实验。

### 按需速查

- `07_正式学习讲义/03_Pod状态与探针_Running为什么不等于Ready.md`
- `07_正式学习讲义/04_Service与EndpointSlice_流量怎么找到Pod.md`
- `07_正式学习讲义/05_资源与弹性_requests_limits_QoS_HPA.md`
- `07_正式学习讲义/06_节点维护与排障_cordon_drain_PDB_NodeReady.md`

### 验收产出

```text
全通过：无额外产出，直接进入 08 scheduler
有缺口：只保存对应一项的证据闭环
```

## 6. 第 3 周：scheduler、资源模型与 Pending

### 目标

把 CPU/memory 的资源调度模型学透，为 `nvidia.com/gpu` 做准备。

本周统一用现有 Java Deployment 的发布与 Pending 现场作为主案例，GPU 只在每课末尾做一次资源维度映射。

### 必须掌握

- request、Node capacity/allocatable、already requested。
- 调度队列、Filter/Score、NodeResourcesFit、assume/bind。
- taint/toleration、nodeSelector、affinity、topology spread。
- Unschedulable、backoff、事件唤醒和抢占边界。
- ResourceQuota 对 extended resource 的预算控制。

GPU 配额示例先记住资源键的形式：

```yaml
spec:
  hard:
    requests.nvidia.com/gpu: "8"
```

### 实验

逐个制造：

```text
Insufficient cpu / memory
taint 不容忍
nodeSelector 不匹配
required affinity 不满足
topology spread 不满足
PDB / preemption 场景
```

每个场景都记录 Pod spec、Node allocatable、Event 和修复动作。

### 正式课将带到的源码范围（现在不用跳旧目录）

- `90_主线复盘与进阶/67_进阶专题_scheduler_Pending到Unschedulable排障链路.md`
- `90_主线复盘与进阶/68_进阶专题_scheduler_NodeResourcesFit与资源请求计算.md`
- `90_主线复盘与进阶/1164_进阶专题_scheduler资源调度总复盘_Profile_Plugin_Filter_Score_Event闭环.md`

### 验收产出

```text
Pod从Pending到绑定排障总图.md
request_limit_allocatable对照实验.md
FailedScheduling故障手册.md
```

## 7. 第 4 周：kubelet、CRI 与节点执行

### 目标

能从 `spec.nodeName` 一直追到 containerd 真正创建容器。

本周继续以 Java Pod 的 ContainerCreating、探针、CrashLoopBackOff 和节点故障作为主案例；重点是 kubelet/CRI 源码运行逻辑，GPU 只标记后续设备注入边界。

### 必须掌握

- syncLoop、podWorkers、SyncPod。
- volume、cgroup、probe、runtime 的准备顺序。
- kubeGenericRuntimeManager 与 CRI。
- PodSandbox、containerd、OCI runtime 的边界。
- statusManager、NodeStatus、NodeLease。
- ContainerCreating、CreateContainerError、CrashLoopBackOff、NotReady 的分层排障。

### 实验

制造并记录：

```text
镜像拉取失败
容器命令错误
readiness 失败
卷挂载失败
containerd 短暂不可用
节点 NotReady 或 drain 场景
```

### 正式课将带到的源码范围（现在不用跳旧目录）

- `90_主线复盘与进阶/1245_进阶专题_kubelet必学边界_从NodeName到容器运行.md`
- `90_主线复盘与进阶/1260_进阶专题_kubelet节点执行主线总复盘_从NodeName到PodRunning.md`
- `90_主线复盘与进阶/1263_进阶专题_CRI远程服务边界_从kubeGenericRuntimeManager到containerd.md`
- `90_主线复盘与进阶/1267_进阶专题_kubelet必学收口地图_哪些必须回看哪些可以暂缓.md`

### 验收产出

```text
Pod从NodeName到容器运行总图.md
kubelet_containerd_CRI边界排障手册.md
节点异常证据采集清单.md
```

## 8. 第 5 周：NVIDIA 单机基础

### 目标

先把 Kubernetes 下面的 GPU 软件栈跑通。

### 必须掌握

- GPU、显存、SM、温度、功耗的基本含义。
- NVIDIA Driver、CUDA 用户态、Container Toolkit、containerd 的关系。
- `nvidia-smi` 能证明什么，不能证明什么。
- 宿主机可见 GPU 但容器不可见时如何分层排查。
- CUDA 镜像与宿主机 Driver 的兼容边界。

### 实验

1. 宿主机执行 `nvidia-smi`。
2. 普通容器与 GPU 容器分别验证设备可见性。
3. 记录 `/dev/nvidia*`、runtime 配置和容器内环境。
4. 故意使用错误 runtime 配置，记录失败现象并恢复。

### 正式课将带到的源码范围（现在不用跳旧目录）

本周不深读 Kubernetes 源码，重点是配置、日志和软件栈边界。

### 验收产出

```text
Driver_CUDA_Toolkit_containerd关系图.md
宿主机有GPU但容器不可见排障SOP.md
GPU单机smoke-test脚本或命令清单.md
```

## 9. 第 6 周：GPU 进入 Kubernetes 的完整链路

### 目标

学穿 `nvidia.com/gpu` 从设备上报到容器可见的全过程。

### 必须掌握

- extended resource 与普通 CPU/memory 的差异。
- Device Plugin 注册、ListAndWatch、健康上报。
- Node capacity/allocatable 如何出现 `nvidia.com/gpu`。
- scheduler 与 kubelet DeviceManager 的职责分界。
- Allocate 返回 env/device/mount/annotation/CDI。
- podDevices/checkpoint 与 PodResources API。

### 实验

1. 查看 Node capacity/allocatable 中的 GPU。
2. 运行一个申请 `limits: nvidia.com/gpu: 1` 的 Pod。
3. 查看 Pod Event、kubelet 和 Device Plugin 日志。
4. 删除/重启 Device Plugin，观察 socket、资源和恢复过程。
5. 验证容器内 GPU UUID，并尝试与 PodResources/监控标签对应。

### 正式课将带到的源码范围（现在不用跳旧目录）

1. `90_主线复盘与进阶/1004_进阶专题_PodResources_DevicePlugin_DRA总复盘_节点设备资源闭环.md`（先看总图）
2. `90_主线复盘与进阶/999_进阶专题_DevicePlugin注册_registerEndpoint与ListAndWatch链路.md`
3. `90_主线复盘与进阶/1000_进阶专题_DeviceManager_allocate设备分配与podDevices状态链路.md`
4. `90_主线复盘与进阶/1001_进阶专题_DeviceManager_checkpoint恢复与健康状态更新链路.md`
5. 再回看 `1004` 收口

### 验收产出

```text
GPU_Pod从声明到容器可见GPU链路.md
GPU_Pod_Pending排障手册.md
DevicePlugin重启恢复实验.md
```

## 10. 第 7 周：GPU Operator 与标准节点池

### 目标

能建设、升级和排查一个标准 GPU 节点池。

### 必须掌握

- GPU Operator 的 ClusterPolicy 与组件关系。
- Driver、Toolkit、Device Plugin、DCGM Exporter、MIG Manager、NFD/GFD、Validator。
- 哪些组件由 Operator 管理，哪些可能由云平台或宿主机预装。
- GPU 型号标签、节点 taint、Pod toleration、affinity 和 RuntimeClass。
- Operator 安装、升级、回滚、drain 和故障隔离。

注意：

```text
申请 nvidia.com/gpu 和容忍 GPU 节点 taint 是两件独立的事。
```

### 实验

1. 安装或审计 GPU Operator。
2. 逐个确认 Operator 组件状态和日志入口。
3. 验证 ClusterPolicy、Node label 和 `nvidia.com/gpu`。
4. 模拟一个组件不可用并按责任层恢复。
5. 写出升级/回滚前的版本兼容检查表。

### 正式课将带到的源码范围（现在不用跳旧目录）

- Kubernetes Controller/Operator 只学机制和关键入口。
- 不逐行读 GPU Operator 全部源码和 CRD。

### 验收产出

```text
GPU节点池组件地图.md
GPU_Operator安装升级回滚SOP.md
GPU节点标签污点与准入规范.md
```

## 11. 第 8 周：DCGM 监控与 GPU 故障演练

### 目标

从“Pod Running”升级到“GPU 和业务真正健康”。

### 必须掌握

- GPU 利用率、显存、温度、功耗、ECC、Xid。
- 节点、GPU、Pod/container、业务四层指标。
- PodResources/GPU UUID 与指标归属。
- GPU util 低不一定浪费，GPU util 高不一定服务健康。
- 何时只重启 Pod，何时隔离/drain Node，何时需要重置或报修 GPU。

### 实验

1. 接入 DCGM Exporter、Prometheus 和 Grafana。
2. 跑空闲、轻载和高负载三组 GPU 工作负载。
3. 制造 Device Plugin down、容器内 GPU 不可见、显存不足等安全故障。
4. 对无法安全制造的 Xid/硬件故障，用历史样本或告警测试数据演练决策树，不破坏硬件。

### 正式课将带到的源码范围（现在不用跳旧目录）

- `90_主线复盘与进阶/995_进阶专题_KubeletPodResources_gRPC服务与设备资源上报链路.md`
- `90_主线复盘与进阶/996_进阶专题_PodResources_List_GetAllocatableResources与Get接口链路.md`

### 验收产出

```text
DCGM核心指标解释.md
GPU_Grafana看板.json
GPU_PrometheusRule.yaml
GPU故障分层与节点隔离SOP.md
```

## 12. 第 9 周：vLLM 最小在线推理服务

### 目标

把 GPU 资源变成真正对外提供服务的工作负载。

### 必须掌握

- 模型镜像、模型权重、PVC/对象存储/节点缓存。
- 模型加载时间、startup/readiness、优雅终止。
- 显存占用、KV cache、并发、batch、tokens/s 和延迟。
- Service/Ingress/Gateway 的超时、长连接和流量边界。

### 实验

1. 选择能在当前 GPU 上运行的小模型。
2. 部署一个最小 vLLM 服务。
3. 记录首次下载、冷启动、模型加载和 Ready 时间。
4. 验证发布、回滚、Pod 重建和节点迁移行为。

### 验收产出

```text
vLLM_K8s部署模板.yaml
模型下载缓存与冷启动记录.md
vLLM发布回滚SOP.md
```

## 13. 第 10 周：压测、SLO 与扩缩容

### 目标

能解释服务延迟和吞吐，而不只看 GPU 利用率。

### 必须掌握

- TTFT、TPOT、端到端延迟、tokens/s、错误率和并发。
- 显存不足、排队、batch、模型大小、CPU/内存、存储和网络对服务的影响。
- 为什么 GPU util 不适合作为唯一扩缩容指标。
- custom/external metrics、Prometheus Adapter 或 KEDA 的边界。
- HPA 扩 Pod 与 GPU Node Pool 扩节点的先后关系。

### 实验

1. 做低、中、高三档并发压测。
2. 同时记录 GPU、Pod、节点和业务指标。
3. 制造 GPU OOM 或安全的显存不足场景并恢复。
4. 尝试一个队列长度、并发或延迟驱动的扩缩容方案。

### 正式课将带到的源码范围（现在不用跳旧目录）

- `90_主线复盘与进阶/1094_进阶专题_HPA主线总复盘_从kubelet指标到scale子资源闭环.md`

### 验收产出

```text
vLLM压测报告.md
推理服务SLO与告警规则.md
GPU_OOM_延迟_吞吐排障手册.md
```

## 14. 第 11 周：多租户、队列、共享与成本

### 目标

从单个服务进入平台治理，但一次只选一套方案。

### 默认选择

- 队列先选 Kueue；目标公司明确使用 Volcano 时再切换。
- 共享先按硬件选择 time-slicing 或 MIG；不同时铺三种方案。
- MIG 只在支持的 GPU 上实验。

### 必须掌握

- Namespace、ResourceQuota、PriorityClass。
- 在线推理与离线/训练任务为什么应分池或分队列。
- Kueue 的 LocalQueue、ClusterQueue、ResourceFlavor 和 quota。
- GPU 型号、Spot/按需、租户、优先级和借用边界。
- MIG、time-slicing、MPS 的隔离性、故障域、监控和计费差异。
- 分配 GPU 时长、有效利用率、空闲率和业务吞吐的成本口径。

### 实验

1. 为两个租户设置 namespace、quota 和 priority。
2. 使用 Kueue 或选定队列做 GPU 预算和准入实验。
3. 选择一种共享方式完成最小实验，记录隔离和监控差异。
4. 输出按团队/模型/节点池归属的成本样例。

### 验收产出

```text
GPU多租户与配额方案.md
Kueue队列与ResourceFlavor实验.yaml
GPU共享方式对比与选型.md
GPU成本治理指标.md
```

## 15. 第 12 周：综合故障演练与作品收口

### 目标

把前 11 周内容变成可验证能力，而不是散落的笔记。

### 综合演练场景

至少完成下面 8 个：

1. GPU Pod 因资源不足 Pending。
2. GPU 节点 taint 没有 toleration。
3. Node 不上报 `nvidia.com/gpu`。
4. Device Plugin down 或注册失败。
5. Pod 已运行但容器内看不到 GPU。
6. GPU OOM 或模型加载失败。
7. 推理延迟升高但 GPU util 不高。
8. 租户配额、队列或优先级阻止任务运行。

每个场景都要保存：

```text
现象
复现配置
kubectl / Event
组件日志
监控指标
根因
恢复步骤
回滚步骤
源码或机制定位
```

### 最终作品

```text
GPU平台架构图
一键部署与清理说明
GPU节点池运维SOP
GPU监控看板与告警
GPU故障手册
vLLM部署与压测报告
多租户与成本方案
一次完整事故复盘
```

## 16. 每周固定动作

每周至少完成：

```text
1 个可执行配置或脚本
1 次成功实验
1 次故障或异常实验
1 份证据记录
1 张链路图
1 页复盘
```

每周复盘只回答六个问题：

1. 我本周真正运行了什么？
2. 我制造了什么故障？
3. 我用什么证据定位？
4. 哪个机制还解释不清？
5. 为此回看了哪一段源码？
6. 下周能复用哪些配置和 SOP？

## 17. 阶段闸门

### 进入 GPU 前

必须能独立处理平台 K8s 的发布、Pending、Creating、NotReady、Service 无后端、节点维护、资源和 HPA 问题。

### 进入 vLLM 前

必须能讲清并验证：

```text
Device Plugin 注册
ListAndWatch
Node allocatable
scheduler 选节点
DeviceManager 选设备
Allocate
runtime 注入
```

### 进入多租户前

必须已有单个 GPU 服务、监控、告警、压测和故障手册；否则队列和成本治理没有真实对象。

## 18. 这 12 周明确不做什么

- 不从第 1 篇顺序读到第 1267 篇。
- 不继续按数量生成 Kubernetes 源码章节。
- 不同时学习 vLLM、Triton、KServe、Ray Serve 和 Seldon。
- 不同时学习 Kueue 和 Volcano 的全部实现。
- 不在硬件不支持时强做 MIG。
- 不把当前 Kubernetes `master` 中的新特性直接当成生产默认。
- 不在第一阶段深挖 DRA、NCCL、RDMA、多机多卡和训练框架源码。
- 不把“看过文档”记为完成；只有可复现证据和验收结果才算完成。

## 19. 官方资料入口

- Kubernetes Device Plugins: <https://kubernetes.io/docs/concepts/extend-kubernetes/compute-storage-net/device-plugins/>
- Kubernetes Extended Resources: <https://kubernetes.io/docs/tasks/configure-pod-container/extended-resource/>
- NVIDIA GPU Operator: <https://docs.nvidia.com/datacenter/cloud-native/gpu-operator/latest/>
- Kueue Concepts: <https://kueue.sigs.k8s.io/docs/concepts/>

这 12 周真正的主线只有一句：

```text
先把普通 Pod 的生产闭环做扎实，
再把 GPU 设备链路接进去，
最后把它变成有监控、有 SLO、有租户和有成本数据的在线推理平台。
```
